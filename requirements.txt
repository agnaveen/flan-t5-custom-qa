# Core libraries
transformers==4.38.2
datasets==2.18.0
torch>=2.0.0
accelerate>=0.26.0

# Optional but useful
tqdm
numpy
scipy

# Tokenizer compatibility
sentencepiece

# To suppress tokenizer warnings
protobuf

# For script execution (if using CLI params in future)
click
